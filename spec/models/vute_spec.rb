module Api
  module V1

    RSpec.describe Vute do
      # it { is_expected.to have_attribute :user }
      #
      # it "validates presence of user" do
      #   subject.user = nil
      #   subject.valid?
      #   expect(subject.errors[:user]).to include "can't be blank"
      # end
    end

  end
end
